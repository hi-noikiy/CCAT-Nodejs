'use strict'

const debug = require('debug')('core:engine:lib:calc')
const mathjs = require('mathjs')

debug('src/core/engine/lib/calc.js is called')

class Calc {
  calcMarkets (markets) {
    debug('calcMarkets is called')
    // to be continue
    return false
  }
  calcSymbol (symbol) {
    debug('calcSymbol is called')
    // to be continue
    return false
  }
  calcSymbols (symbols) {
    debug('calcSymbols is called')
    // to be continue
    return false
  }
  calcCurrencies (currencies) {
    debug('calcCurrencies is called')
    // to be continue
    return false
  }
  calcTicker (ticker) {
    debug('calcTicker is called')
    // to be continue
    return false
  }
  calcTickers (tickers) {
    debug('calcTickers is called')
    // to be continue
    return false
  }
  calcDepth (depth) {
    debug('calcDepth is called')
    // to be continue
    return false
  }
  calcOrder (order) {
    debug('calcOrder is called')
    // to be continue
    return false
  }
  calcOrders (orders) {
    debug('calcOrders is called')
    // to be continue
    return false
  }
  calcTrades (trades) {
    debug('calcTrades is called')
    // to be continue
    return false
  }
  calcBalance (balance) {
    debug('calcBalance is called')
    // to be continue
    return false
  }
}

module.exports = Calc
